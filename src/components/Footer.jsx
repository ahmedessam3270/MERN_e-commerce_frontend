function Footer() {
  return (
    // how to make this footer sticky at the bottom of the page

    // Solution:
    // Add the class `fixed` to the footer element
    // Add the class `bottom-0` to the footer element
    // Add the class `w-full` to the footer element
    // Add the class `bg-slate-200` to the footer element
    // Add the class `p-4` to the footer element
    // Add the class `text-center` to the paragraph element
    // Add the class `font-bold` to the paragraph element

    // <footer className="fixed bottom-0 w-full bg-slate-200 p-4">
    //   <p className="text-center font-bold">Created with love by Ahmed</p>
    // </footer>
    <footer className="fixed bottom-0 w-full bg-slate-200 p-4">
      <p className="text-center font-bold">Created by Ahmed</p>
    </footer>
  );
}

export default Footer;
